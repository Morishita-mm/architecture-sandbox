# Review source code and examples, not local deployment state or credentials.
.git/
.terraform/
*.tfstate*
*.tfplan
*.tfvars
*.tfvars.json
backend.hcl
.dev.vars*
.wrangler/
# Short-lived GitHub OIDC credential files must never enter artifacts.
gha-creds-*.json
